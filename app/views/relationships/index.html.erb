<div class="main-panel-container h-screen relative py-5 minimal-scrollbar">
  <div class="flex items-center justify-between sticky-header">
    <div>
      Relationships
    </div>
    <%= form_for :search, url: relationships_path, method: :get, class: "flex items-center" do |f| %>
      <%= f.text_field :query, value: @query, class: "h-10 border border-gray-300 rounded-md p-1.5 mr-2", placeholder: "Find by name" %>
      <%= f.submit "Search", class: "bg-blue-300 py-1.5 px-4 rounded cursor-pointer" %>
    <% end %>
  </div>

  <div class="flex flex-col sm:grid sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-5 mt-5">
    <% @relationships.each do |relationship| %>
      <%= link_to relationship_path(relationship) do %>
        <div class="white-panel">
          <div class="flex justify-between text-xl mb-4">
            <%= relationship.name %>
          </div>
          <div>
            Entries: <%= relationship.relationship_summaries.count %>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
