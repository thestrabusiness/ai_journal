<!DOCTYPE html>
<html>
  <head>
    <title>AiJournal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="fixed inset-0 bg-gray-100 mb-10 <%= "animate-slide-down-up" if notice.present? %>">
    <% if notice.present? %>
      <div
        class="flex w-full items-center justify-center h-14 bg-green-200 text-green-600 rounded-b-lg shadow-lg"
        id="notice">
        <%= notice %>
      </div>
    <% end %>
    <main class="flex h-full items-start">
      <div class="flex flex-col h-full my-5 mr-5 p-5 w-20 items-center bg-white rounded-r-lg shadow-xl">
        <%= link_to "Home", root_path, class: "h-5 mb-5" %>
        <%= link_to "Entries", journal_entries_path, class: "h-5 mb-5" %>
      </div>
      <%= yield %>
    </main>
  </body>
</html>
