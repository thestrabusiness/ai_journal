<!DOCTYPE html>
<html>
  <head>
    <title>AiJournal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="sm:fixed sm:inset-0 sm:mb-10 mobile-portrait:mb-0 bg-gray-100 <%= "animate-slide-down-up" if notice.present? %>">
    <% if notice.present? %>
      <div
        class="flex w-full items-center justify-center h-14 bg-green-200 text-green-600 rounded-b-lg shadow-md"
        id="notice">
        <%= notice %>
      </div>
    <% end %>
    <main class="flex flex-col-reverse sm:flex-row h-screen sm:h-full items-start">
      <nav class="nav-bar">
        <%= render "nav_link", path: root_path, icon_name: "edit-3" %>
        <%= render "nav_link", path: journal_entries_path, icon_name: "book-open" %>
        <%= render "nav_link", path: relationships_path, icon_name: "users" %>
        <%= render "nav_link", path: questions_path, icon_name: "search" %>
        <%= render "nav_link", path: core_values_path, icon_name: "heart" %>
        <%= render "nav_link", path: pulse_check_path, icon_name: "activity" %>
      </nav>
      <%= yield %>
      <%= render "chat_logs/sidebar" %>
    </main>
  </body>
</html>
